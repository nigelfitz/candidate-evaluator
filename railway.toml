[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd flask_app && gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 'app:create_app()'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
